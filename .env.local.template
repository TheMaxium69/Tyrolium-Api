# .env.local.template — copie-le en .env.local et adapte les valeurs à ta config locale.
# cp .env.local.template .env.local
#
# Ce fichier n'est jamais lu par Symfony (contrairement à .env.local) — c'est juste un
# point de départ committé pour ne pas repartir de zéro. Voir .doc/env-files.md.

###> doctrine/doctrine-bundle ###
DATABASE_URL="mysql://root:root@127.0.0.1:8889/tyrolium_api?serverVersion=8.0.35&charset=utf8mb4"
###< doctrine/doctrine-bundle ###
