
###> nelmio/cors-bundle ###
CORS_ALLOW_ORIGIN='^https://(hub\.tyrolium\.fr|dashboard\.useritium\.fr)$'
###< nelmio/cors-bundle ###

###> doctrine/doctrine-bundle ###
# Pas de DATABASE_URL ici : mot de passe = secret. Valeur réelle définie en variable
# d'environnement serveur ou via le Secrets Vault (bin/console secrets:set), jamais dans
# ce fichier committé. Voir .doc/env-files.md. Cluster cible (cahier des charges) :
# database701.tyrolium.fr / database702.tyrolium.fr — moteur MySQL (décidé le 2026-08-09).
###< doctrine/doctrine-bundle ###
